# 统一换行符，避免 Windows / macOS / Linux 之间产生整文件级别的假 diff
* text=auto eol=lf

# Windows 批处理用 CRLF 更稳妥
*.cmd text eol=crlf
*.bat text eol=crlf

# 二进制文件不做换行符转换
*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.gif binary
*.ico binary
